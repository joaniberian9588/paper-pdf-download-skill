# Keep MinerU conversion local by default

The project supports both local MinerU and MinerU Open API, but verified papers—especially institutionally licensed PDFs—are converted locally unless the user explicitly selects API mode. This trades a heavier local installation for a clear privacy boundary while retaining an opt-in hosted path for users who value convenience over local processing.
